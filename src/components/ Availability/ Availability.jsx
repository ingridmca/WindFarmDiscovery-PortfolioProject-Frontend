import styled from "styled-components";

const Availability = () => {
  return <Box> Availability component</Box>;
};

export default Availability;

const Box = styled.div`
  display: flex;
  width: 500px;
  height: 500px;
  background: #dcdcdc;
  flex-direction: row;
  justify-content: space-around;
`;
